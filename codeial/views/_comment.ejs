<li id="post-<%=  post._id%>">
    <p id="commentss">
        <% if(locals.user && locals.user.id == comment.user.id){ %>
            <small>
                <a class="delete-comment-button" href="/comments/destroy/<%= comment.id %>">X</a>
            </small>
        <%}%> 
       
        <%= comment.content %>
        
        <small id="comments-section">
            <%= comment.user.name%>
        </small>
    </p>
</li>